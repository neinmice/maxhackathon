# MAX Bot service

Boundary for MAX Bot API handlers. Official MAX research confirms Mini Apps work inside chat bots, including the open_app button and configured quick-launch button. Implement after checking the current client/assignment. The bot calls the shared API/catalog and never contains a second copy of business rules.
