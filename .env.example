APP_ENV=development
API_PORT=8000
POSTGRES_DB=navigator
POSTGRES_USER=navigator
POSTGRES_PASSWORD=navigator
MAX_BOT_TOKEN=replace-in-secret-store
MAX_API_BASE_URL=https://platform-api2.max.ru
MAX_BOT_USERNAME=replace-with-bot-username
MAX_WEBHOOK_SECRET=replace-with-a-random-32-byte-secret
PUBLIC_DOMAIN=zverybot.ru
PUBLIC_BASE_URL=https://zverybot.ru
MAX_LAUNCH_MAX_AGE_SECONDS=3600
CERTIFICATE_SIGNING_SECRET=replace-with-a-separate-random-32-byte-secret
# Product-owned quiz content. Keep this empty until the approved answer key is available.
QUIZ_ANSWER_KEY_JSON=
QUIZ_PASS_SCORE=70
# Path inside the bot container to a CA bundle, only if the host does not trust MAX's CA.
MAX_CA_BUNDLE_PATH=
